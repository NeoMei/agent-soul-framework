# @neomei/agent-soul

> 已合并到 `@neomei/agent-soul-framework`

灵魂注入插件现在是魂器核心框架的内置组件，位于 `@neomei/agent-soul-framework/plugin/`。

安装核心框架即可使用：

```bash
npm install -g @neomei/agent-soul-framework
```

该包保留为空包，用于避免旧安装命令报错，不再单独维护。
